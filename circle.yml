machine:
  node:
    version: 0.10.33

test:
  override:
   - make test

deployment:
  publish:
    owner: segmentio
    tag: /[0-9]+(\.[0-9]+)*(-.+)?/
    commands:
      - npm config set "//registry.npmjs.org/:_authToken" $NPM_AUTH
      - npm publish .
